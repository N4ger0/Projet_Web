{% if app.user == null %}
<img src="{{ asset('BangerMovies.png') }}"/>
{% elseif app.user.roles == ['ROLE_CLIENT'] %}
<img src="{{ asset('BangerMoviesClient.png') }}"/>
{% elseif app.user.roles == ['ROLE_ADMIN'] or app.user.roles == ['ROLE_SADMIN'] %}
<img src="{{ asset('BangerMoviesAdmin.png') }}"/>
{% endif %}